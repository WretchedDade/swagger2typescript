{{#StripSingleQuotes}}
import {{typesNamespace}} from './{{typesNamespace}}';

export default class {{name}}Service {
{{#each endpoints}}

/**
* {{{summary}}}
*/
public async {{endpoint}}({{{request}}}): Promise<{{responseBodyType}}> {
    const response = await fetch('');
    return response.json();
    }
    {{/each}}
    }
    {{/StripSingleQuotes}}